{
  "name": "developstoday-task",
  "version": "1.0.0",
  "description": "The back-end is developed with NodeJS and ExpressJS. It will run on port 3000. The front-end is a NextJS app. It should run on port 3001, in order to do that, run the server first followed by the client.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
